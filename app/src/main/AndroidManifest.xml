<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.eslamypunis.nvdrawer">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/cinema"
        android:label="Movies Guide"
        android:roundIcon="@drawable/cinema"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="Movies Guide"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ViewInfo" />
        <activity android:name=".FullScreenImage" />
        <activity android:name=".SearchMovies" />
        <activity android:name=".ReviewsActivity" />
        <activity android:name=".ViewAllReviews" />
        <activity android:name=".BookingMovies" />
        <activity android:name=".FavouritsActivity" />
    </application>

</manifest>